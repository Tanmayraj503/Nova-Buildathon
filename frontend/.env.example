# Only needed if the frontend and backend are NOT served from the same origin
# (e.g. deploying the frontend separately from the Express API). In local dev,
# Vite's proxy (see vite.config.js) already forwards /api/* to localhost:4000,
# so this can usually stay unset.
# VITE_API_BASE_URL=https://your-backend.example.com
